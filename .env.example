# Supabase (optional). Leave both empty to run in local demo mode (all data stays in this browser).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Site URL used for magic-link redirects (e.g. https://life.example.com). Defaults to the request origin.
NEXT_PUBLIC_SITE_URL=
